node_modules
dist
.DS_Store
*.local

# Entorno / secretos — nunca subir
.env
.env.*
!.env.example

# Vercel
.vercel

# Videos de referencia (Figma), no van al repo
mov/

# Docs de referencia (Figma / guion), no van al repo
pdf/
